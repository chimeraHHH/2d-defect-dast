% !TEX root = ../main.tex

\ifprmresultsready

We established a provenance-audited evaluation of a defect-aware graph model
for neutral impurity incorporation energies in two-dimensional materials.  A
paired $2^3$ experiment selected \PRMSelectedVariant using validation data
only, after which the same architecture was evaluated on random, pair-, host-,
impurity-, and chemistry-block partitions against split-aligned SchNet and
descriptor baselines.  The corresponding DART MAEs were
\PRMIdCvDARTMAE, \PRMPairCvDARTMAE, \PRMHostCvDARTMAE,
\PRMDopantCvDARTMAE, and \PRMChemistryBlockDARTMAE\eV{}.

On a separate held-out uncertainty split, 90\% conformal intervals achieved
\PRMUQNinetyCoverage\% marginal coverage at mean width
\PRMUQNinetyWidth\eV{}.  Pair-held-out predictions recovered the preferred
incorporation class with \PRMPreferenceAccuracy\% accuracy and incurred mean
global site-selection regret \PRMGlobalRegret\eV{}.  Together, these results
define both the predictive utility and the observable failure modes of the
surrogate within IMP2D.  They do not constitute prospective first-principles
validation or evidence for materials outside the audited chemical domain.

\else

\noindent\textit{The numerical conclusion is withheld until canonical result
assets pass the complete provenance and coverage contract.}

\fi
